version: 2
models:
  - name: stg_tickit__category
    columns:
      - name: category_id
        data_type: smallint
      - name: category_group
        data_type: varchar(64)
      - name: category_name
        data_type: varchar(64)
      - name: category_description
        data_type: varchar(256)
