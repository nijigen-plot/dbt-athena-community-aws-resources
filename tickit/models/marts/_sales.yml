version: 2
models:
  - name: sales
    columns:
      - name: year
        data_type: smallint
      - name: month
        data_type: varchar(32)
      - name: commission
        data_type: decimal(38,2)
        data_tests:
          - not_null
